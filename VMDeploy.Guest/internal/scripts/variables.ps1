# List of actions available when executing the Guest OS configuration
$script:actions = @{ }

# List of configuration entries that are applied when executing Invoke-VMGuestConfiguration
$script:configurations = @{ }